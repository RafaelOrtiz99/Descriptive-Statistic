#print(x,n) muesta las n cifras significativas de x

# round(x,n) Redondea a n cifras significativas un resultado o vector númerico x

# floor(x) parte entera por defecto de x

# ceiling(x) parte entera por exceso de x

# trunc(x) parte entera de x, eliminando la parte decimal

sqrt(2)

print(sqrt(2), 10)

round(sqrt(2), 3)

floor(sqrt(2))

ceiling(sqrt(2))

trunc(sqrt(2))

